[package]
name = "tempfile"

# Pre-installed spin package: ships with the compiler, versioned with it.
# `Tempfile.create` -- plain Ruby over the tmpdir package and File's
# exclusive-create mode. See tempfile.rb for what is deliberately absent.
